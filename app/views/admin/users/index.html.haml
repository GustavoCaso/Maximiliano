%h1 Listing admin_foos

%table
  %tr
    %th Username
    %th Name
    %th Email
    %th Password
    %th
    %th
    %th

  - @users.each do |user|
    %tr
      %td= user.username
      %td= user.name
      %td= user.email
      %td= user.password
      %td= link_to 'Show', [:admin, user]
      %td= link_to 'Edit', edit_admin_user_path(user)
      %td= link_to 'Destroy', user, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Foo', new_admin_user_path
