%h1 Listing orders

%table
  %tr
    %th Name
    %th Email
    %th Address
    %th
    %th
    %th

  - @orders.each do |order|
    %tr
      %td= order.name
      %td= order.email
      %td= order.address
      %td= link_to 'Show', order
      %td= link_to 'Edit', edit_order_path(order)
      %td= link_to 'Destroy', order, :method => :delete, :data => { :confirm => '¿ Estas seguro de querer borrarlo ?' }

%br

= link_to 'Pedido Nuevo', new_order_path
